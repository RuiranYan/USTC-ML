1、原先的可视化运行很慢，需要手动中断运行，目前新改进的版本可视化运行很快即可结束并保存结果，不需要手动中断。
2、数据预处理时把英语字符过滤了，如果想要分析英语文本，启动或者注释这两行代码
word = re.sub(u'[^\u4e00-\u9fa5]','',seg_word.word)
#word = seg_word.word  #如果想要分析英语文本，启动这行代码(去掉行首的“#”，注释上一行代码（行首添加“#”）